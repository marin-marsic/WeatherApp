<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="eu.fiveminutes.android.weatherapp.MainActivity">

    <LinearLayout
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:layout_width="@dimen/zero"
        android:layout_height="@dimen/zero"/>

    <EditText
        android:id="@+id/city_query"
        android:hint="@string/search_for_the_city"
        android:layout_width="fill_parent"
        android:inputType="text"
        android:nextFocusUp="@id/city_query"
        android:nextFocusLeft="@id/city_query"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:layout_toLeftOf="@+id/search_button"
        android:layout_height="@dimen/header_height"
        android:layout_marginRight="@dimen/padding"
        android:layout_marginLeft="@dimen/padding"
        android:layout_marginBottom="@dimen/padding" />

    <ImageView
        android:id="@+id/search_button"
        android:layout_width="@dimen/search_icon_width"
        android:layout_height="@dimen/search_icon_height"
        android:background="@drawable/search_icon"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/padding"
        android:layout_marginLeft="@dimen/padding"
        android:layout_marginBottom="@dimen/padding" />

    <TextView
        android:id="@+id/header"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/header_height"
        android:gravity="center_vertical"
        android:background="@drawable/header_sky"
        android:textColor="@color/header_font"
        android:textSize="@dimen/header_font_size"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:layout_below="@+id/search_button"
        android:text="@string/header" />

    <TextView
        android:id="@+id/error"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/header"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:gravity="center_vertical" />

    <android.support.v4.widget.SwipeRefreshLayout
        android:id="@+id/index_swipe_refresh"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/header">

    <ListView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/listview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/header"
        android:layout_weight="1"/>

    </android.support.v4.widget.SwipeRefreshLayout>

</RelativeLayout>
